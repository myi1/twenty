// FactsRail.tsx: STUB. Filled in by Task 10 (deal fields, lead picks, the lead
// picture). Typed against exactly what index.tsx passes so the entry compiles
// now; the real component replaces this whole file.

import type { PropelHeroHost } from '@/propel/runtime/heroHost';
import type { LeadLoad } from './types';

export const FactsRail = (_props: {
  host: PropelHeroHost;
  data: LeadLoad;
  onChanged: () => void;
}) => null;
